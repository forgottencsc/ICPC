#include <bits/stdc++.h>
using namespace std;

int main(void) {
    #ifndef ONLINE_JUDGE
    freopen("1.in", "r", stdin);
    freopen("1.out", "w", stdout);
    #endif // ONLINE_JUDGE

    for (int u = -1; u <= 1; u += 2)
        for (int v = -1; v <= 1; v += 2)



    return 0;
}